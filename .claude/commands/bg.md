Run the following task in the background as a fire-and-forget job. Use the Agent tool with `run_in_background: true` and pass the task description below as the prompt. After dispatching, confirm to the user that the job was sent to the background.

Task: $ARGUMENTS

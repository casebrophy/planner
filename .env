# Non-secret configuration (committed to git)
# Secrets are in .secrets.env (SOPS-encrypted)

PLANNER_SMTP_ENABLED=false
PLANNER_SMTP_DOMAIN=yourdomain.com
PLANNER_ANTHROPIC_MODEL=claude-sonnet-4-20250514
